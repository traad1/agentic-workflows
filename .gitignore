# Secrets — never commit these
.env
credentials.json
token.json

# Temporary processing files
.tmp/*
!.tmp/.gitkeep

# Python
__pycache__/
*.pyc
*.pyo

# macOS
.DS_Store
service-account.json

# Streamlit
.streamlit/secrets.toml

# App data (user-entered prices — kept locally, not in repo)
VG Platform/dashboard/data/*.xlsx
VG Platform/dashboard/data/*.csv
